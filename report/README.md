# Report

Place the rendered project report here as `movie_industry_clustering.pdf`.

The report is the executed/exported version of the R notebook and includes data summaries, clustering results, tables, and plots.
